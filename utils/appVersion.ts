// ── Installed app identity ────────────────────────────────────────────────────
// Keep in sync with android.versionCode in app.json on every release.
// Used by Settings → About and the App Update screen to compare against
// GET /updates/latest.
export const APP_VERSION = "1.0.4";
export const APP_VERSION_CODE = 1;
